MONGODB_URI=mongodb+srv://<db_username>:<db_password>@studentdata.8xhz9we.mongodb.net/?retryWrites=true&w=majority&appName=StudentData
PORT=3000
# Public base URL used to embed in QR codes (must be reachable by scanners)
# Example for local dev with a tunnel: https://your-ngrok-subdomain.ngrok.io
BASE_URL=http://localhost:3000

